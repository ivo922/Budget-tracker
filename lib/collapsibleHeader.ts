/** Visible header row below the status bar (excluding safe area). */
export const HEADER_CONTENT_HEIGHT = 48;

/** Scroll distance (px) over which the title fully fades out. */
export const HEADER_TITLE_FADE_DISTANCE = 72;

/** Title vertical offset (px) at full scroll — negative moves up. */
export const HEADER_TITLE_TRANSLATE_Y = -20;

/** Scroll distance (px) over which the expanded left action fades into the circular icon. */
export const HEADER_ACTION_FADE_DISTANCE = 56;
